import {
  DatabaseTransactionConnection,
  z,
  sql,
} from "@qte/elephas-migrate-core";

export const up = async (transaction: DatabaseTransactionConnection) => {};

export const down = async (transaction: DatabaseTransactionConnection) => {};
